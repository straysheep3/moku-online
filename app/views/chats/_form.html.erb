<%= simple_form_for [@room, @room.chats.build] do |f| %>
  <%= f.input :comment, class: "form-control" %>
  <p class="text-right">
    <%= f.button :submit, class: "btn btn-primary" %>
  </p>
<% end %>
